export function num(v){const n=Number(v);return Number.isFinite(n)?n:null}
function clean(pairs){return pairs.map(x=>Array.isArray(x)?x:[x.reference,x.phonacore]).map(x=>[num(x[0]),num(x[1])]).filter(x=>x[0]!==null&&x[1]!==null)}
export function pearson(pairs){const p=clean(pairs),n=p.length;if(n<2)return null;const a=p.map(x=>x[0]),b=p.map(x=>x[1]),ma=a.reduce((s,x)=>s+x,0)/n,mb=b.reduce((s,x)=>s+x,0)/n,sa=Math.sqrt(a.reduce((s,x)=>s+(x-ma)**2,0)),sb=Math.sqrt(b.reduce((s,x)=>s+(x-mb)**2,0));return sa&&sb?a.reduce((s,x,i)=>s+(x-ma)*(b[i]-mb),0)/(sa*sb):null}
export function ccc(pairs){const p=clean(pairs),n=p.length;if(n<2)return null;const a=p.map(x=>x[0]),b=p.map(x=>x[1]),ma=a.reduce((s,x)=>s+x,0)/n,mb=b.reduce((s,x)=>s+x,0)/n,va=a.reduce((s,x)=>s+(x-ma)**2,0)/n,vb=b.reduce((s,x)=>s+(x-mb)**2,0)/n,cov=a.reduce((s,x,i)=>s+(x-ma)*(b[i]-mb),0)/n;return 2*cov/(va+vb+(ma-mb)**2)}
export function criterionValidity(pairs){const p=clean(pairs),n=p.length;if(!n)return{n:0,pearson:null,ccc:null,bias:null,mae:null,rmse:null,loa95:null,meanReference:null,meanPhonaCore:null};const d=p.map(x=>x[1]-x[0]),bias=d.reduce((s,x)=>s+x,0)/n,mae=d.reduce((s,x)=>s+Math.abs(x),0)/n,rmse=Math.sqrt(d.reduce((s,x)=>s+x*x,0)/n),sd=n>1?Math.sqrt(d.reduce((s,x)=>s+(x-bias)**2,0)/(n-1)):0;return{n,pearson:pearson(p),ccc:ccc(p),bias,mae,rmse,loa95:[bias-1.96*sd,bias+1.96*sd],meanReference:p.reduce((s,x)=>s+x[0],0)/n,meanPhonaCore:p.reduce((s,x)=>s+x[1],0)/n}}
export function pairReference(rows,{id='sample_id',reference='reference',phonacore='phonacore'}={}){const m=new Map();for(const r of rows){const key=String(r[id]??'');const a=num(r[reference]),b=num(r[phonacore]);if(!key||a===null||b===null)continue;m.set(key,{id:key,reference:a,phonacore:b})}return [...m.values()]}
export function validityByParameter(rows,parameters){return parameters.map(parameter=>{const pairs=pairReference(rows,{id:'sample_id',reference:'reference_'+parameter,phonacore:'phonacore_'+parameter});return{parameter,pairs,...criterionValidity(pairs)}})}
