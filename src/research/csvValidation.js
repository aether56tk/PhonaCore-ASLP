export const VALIDATION_COLUMNS=["participant_id","sample_id","task","device","browser","microphone","sample_rate","duration_sec","quality","securavox_f0","reference_f0","securavox_jitter","reference_jitter","securavox_shimmer","reference_shimmer","securavox_cpp","reference_cpp"];
export function escapeCSV(v){const s=v==null?"":String(v);return /[",\n]/.test(s)?`"${s.replaceAll('"','""')}"`:s}
export function rowsToCSV(rows=[]){const head=VALIDATION_COLUMNS.join(","),body=rows.map(r=>VALIDATION_COLUMNS.map(k=>escapeCSV(r?.[k])).join(","));return [head,...body].join("\n")}
export function parseCSV(text){const lines=String(text||"").split(/\r?\n/).filter(Boolean);if(!lines.length)return [];const split=line=>{const out=[];let cur="",q=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'){if(q&&line[i+1]==='"'){cur+='"';i++}else q=!q}else if(c===","&&!q){out.push(cur);cur=""}else cur+=c}out.push(cur);return out};const h=split(lines[0]);return lines.slice(1).map(line=>{const v=split(line),o={};h.forEach((k,i)=>o[k]=v[i]??"");return o})}