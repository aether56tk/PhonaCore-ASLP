export const SESSION_STATES=["created","consent-pending","ready","active","paused","completed","cancelled"];
export function createSecureSession({patientId,clinicianId,consentScope=[]}={}){const now=new Date().toISOString();return {id:crypto.randomUUID(),patientId,clinicianId,state:"consent-pending",consent:{scope:consentScope,granted:false,grantedAt:null,revokedAt:null},createdAt:now,updatedAt:now,events:[{type:"created",at:now}]}}
export function grantConsent(s,scope=s?.consent?.scope||[]){const now=new Date().toISOString();return {...s,state:"ready",updatedAt:now,consent:{...s.consent,scope,granted:true,grantedAt:now,revokedAt:null},events:[...(s.events||[]),{type:"consent-granted",at:now,scope}]}}
export function revokeConsent(s){const now=new Date().toISOString();return {...s,state:"cancelled",updatedAt:now,consent:{...s.consent,granted:false,revokedAt:now},events:[...(s.events||[]),{type:"consent-revoked",at:now}]}}
export function transition(s,next){if(!SESSION_STATES.includes(next))throw new Error("Invalid session state");const now=new Date().toISOString();return {...s,state:next,updatedAt:now,events:[...(s.events||[]),{type:"state",from:s.state,to:next,at:now}]}}