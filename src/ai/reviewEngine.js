export function reviewMeasurements(m){const flags=[];if(Number.isFinite(m?.qualityScore)&&m.qualityScore<60)flags.push({code:"LOW_QUALITY",severity:"high",message:"Signal quality is low; repeat acquisition before interpretation."});if(Number.isFinite(m?.clippingPercent)&&m.clippingPercent>1)flags.push({code:"CLIPPING",severity:"high",message:"Clipping detected; repeat recording with lower input level."});if(Number.isFinite(m?.voicedPercent)&&m.voicedPercent<30)flags.push({code:"LOW_VOICING",severity:"medium",message:"Limited voiced material; cycle-based measures may be unstable."});if(!Number.isFinite(m?.f0Mean))flags.push({code:"NO_F0",severity:"medium",message:"No reliable F0 estimate; do not interpret pitch-derived metrics."});return {type:"advisory",diagnostic:false,flags,generatedAt:new Date().toISOString()}}
export function compareSessions(previous,current){const keys=["f0Mean","jitter","shimmer","cpp"],changes={};for(const k of keys){const a=Number(previous?.[k]),b=Number(current?.[k]);if(Number.isFinite(a)&&Number.isFinite(b))changes[k]={absolute:b-a,percent:a!==0?((b-a)/Math.abs(a))*100:null}}return {type:"descriptive-longitudinal",diagnostic:false,changes}}