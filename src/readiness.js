export function readinessGates({softwareTests=false,build=false,protocol=false,ethics=false,data=false,referenceData=false,validation=false,securityReview=false}={}){const gates={softwareTests,build,protocol,ethics,data,referenceData,validation,securityReview};const missing=Object.entries(gates).filter(([,v])=>!v).map(([k])=>k);return {readyForClinicalDeployment:missing.length===0,missing,gates}}